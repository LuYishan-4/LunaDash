# Florist

`florist.png` is an AI-generated wallpaper made for LuDash with OpenAI image generation.
It uses the user's desktop reference for its muted florist-street atmosphere, with an original composition and no embedded UI.
The reference screenshot itself is not bundled. Users can select their own local image in desktop settings.
