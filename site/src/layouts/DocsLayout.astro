---
import "../styles.css";
import "../docs.css";
interface Props { title: string; summary: string; section: string; }
const { title, summary, section } = Astro.props;
const base = import.meta.env.BASE_URL.replace(/\/?$/, "/");
const pages = [["settings", "Desktop settings"], ["modules", "Build your shell"], ["plugins", "Plugin SDK"], ["api", "API reference"], ["start", "Run & test"], ["architecture", "Source architecture"]];
---
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" /><meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="description" content={summary} /><meta name="keywords" content="LunaDash, Linux desktop environment, Wayland desktop, Quickshell, Quickshell QML, wlroots, Qt 6, Linux desktop" /><meta name="theme-color" content="#101b1d" />
  <title>{title} · LunaDash Docs</title><link rel="icon" href={`${base}assets/mark.svg`} />
</head>
<body>
<a class="skip" href="#content">Skip to content</a>
<header class="nav wrap"><a class="brand" href={base}><img src={`${base}assets/mark.svg`} width="28" height="28" alt="" /> LunaDash <span class="docs-badge">GUIDES</span></a><nav aria-label="Main navigation"><a href={base}>Desktop</a><a href={`${base}releases/`}>Release notes</a><a class="source" href="https://github.com/LuYishan-4/LunaDash/tree/dev">Source ↗</a></nav></header>
<div class="docs-layout wrap">
  <aside class="docs-sidebar"><span class="eyebrow">MAKE YOURSELF AT HOME</span><nav aria-label="Documentation">{pages.map(([id, label]) => <a href={`${base}docs/${id}/`} aria-current={section === id ? "page" : undefined}>{label}<span aria-hidden="true">↗</span></a>)}<a href={`${base}releases/`}>Release notes<span aria-hidden="true">↗</span></a></nav><p>Development preview<br />Arch, Debian/Ubuntu, Fedora and openSUSE install paths.</p><a class="text-link" href="https://github.com/LuYishan-4/LunaDash/blob/dev/docs/en/TESTING_AND_FILES.md">Testing and source map ↗</a></aside>
  <main id="content" class="docs-main"><div class="eyebrow">LUDASH / {section.toUpperCase()}</div><h1>{title}</h1><p class="docs-lead">{summary}</p><slot /></main>
</div>
<footer class="wrap"><a class="brand" href={base}>LunaDash</a><span>Small details. Your workspace.</span><a href={`${base}releases/`}>Release notes ↗</a></footer>
</body>
</html>
