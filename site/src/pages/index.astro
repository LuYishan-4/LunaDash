---
import "../styles.css";
import "../docs.css";
import CodeBlock from "../components/CodeBlock.astro";

const base = import.meta.env.BASE_URL.replace(/\/?$/, "/");
const asset = (name: string): string => `${base}assets/${name}`;
const repository = "https://github.com/LuYishan-4/LunaDash";
const profile = "https://github.com/LuYishan-4";
const avatar = "https://luyishan-4-blog.wuming381906.workers.dev/assets/images/avatar.webp";

const dependencies = `./scripts/install-dependencies.sh --dry-run
./scripts/install-dependencies.sh`;
const build = `git clone --branch dev ${repository}.git
cd LunaDash
./scripts/install-session.sh`;
const shortcuts: Array<[string, string]> = [
  ["Super + Return / E / D", "Default terminal / file manager / launcher"],
  ["Super + H / L", "Focus the column to the left / right"],
  ["Super + K / J", "Focus the previous / next member inside the column"],
  ["Super + Shift + H / L", "Merge the focused window into the left / right column"],
  ["Super + Shift + E", "Expel the focused member into its own column"],
  ["Super + Ctrl + H / L", "Move the focused column left / right"],
  ["Super + = / -", "Widen / narrow the focused column"],
  ["Super + Shift + C", "Center the focused column"],
  ["Super + F", "Maximize or restore the window under the pointer"],
  ["Super + C / Q", "Close the focused window"],
  ["Super + M", "Minimize the focused window"],
  ["Super + Space", "Toggle floating for the focused window"],
  ["Super + 1–9", "Switch workspace"],
  ["Super + Shift + 1–9", "Move the focused window to a workspace"]
];
---
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="description" content="LunaDash is a Wayland desktop built with C++20, OpenGL and Quickshell, with installation paths for Arch, Debian/Ubuntu, Fedora and openSUSE.">
  <meta name="theme-color" content="#101b1d">
  <title>LunaDash — A moonlit, focused Linux desktop</title>
  <link rel="icon" href={asset("mark.svg")} type="image/svg+xml">
  <script>import "../app";</script>
</head>
<body>
<div id="boot" aria-hidden="true">
  <div class="boot-lockup">
    <svg class="boot-mark" viewBox="0 0 126 126" aria-hidden="true">
      <path class="boot-orbit" d="M18 72 C24 103 58 119 88 104 C111 93 121 67 112 43" />
      <path class="boot-moon" d="M65 11 C38 17 23 43 31 69 C39 96 70 110 95 97 C77 96 60 85 53 68 C44 47 50 25 65 11 Z" />
      <path class="boot-star-a" d="M92 19 L95 27 L103 30 L95 33 L92 41 L89 33 L81 30 L89 27 Z" />
      <path class="boot-star-b" d="M105 54 L107 59 L112 61 L107 63 L105 68 L103 63 L98 61 L103 59 Z" />
      <path class="boot-star-c" d="M78 8 L80 12 L84 14 L80 16 L78 20 L76 16 L72 14 L76 12 Z" />
    </svg>
    <span class="boot-word">LunaDash</span>
  </div>
</div>
<a class="skip" href="#main">Skip to content</a>
<header class="nav wrap">
  <a class="brand" href="#main" aria-label="LunaDash home"><img src={asset("mark.svg")} alt="" width="28" height="28"> LunaDash</a>
  <nav aria-label="Main navigation"><a href="#install">Installation</a><a href="#start">Getting started</a><a href={`${base}releases/`}>Release notes</a><a href={`${base}docs/start/`}>Guides</a><a class="source" href={repository}>GitHub <span aria-hidden="true">↗</span></a></nav>
</header>
<main id="main">
  <section class="hero wrap">
    <img class="banner" src={asset("banner.svg")} alt="LunaDash wordmark beside the crescent moon application mark" width="1600" height="400" fetchpriority="high">
    <h1 class="tagline">A moonlit, focused Linux desktop.</h1>
    <p class="hero-note">A Wayland desktop with scrollable window columns and a customizable Quickshell shell. Your workspaces, apps and everyday controls, together.</p>
    <div class="actions">
      <a class="button primary" href="#install">Installation <span aria-hidden="true">↓</span></a>
      <a class="text-link" href={`${base}releases/`}>Release notes ↗</a>
      <a class="text-link" href={repository}>Source on GitHub ↗</a>
    </div>
  </section>

  <section id="install" class="wrap section">
    <span class="eyebrow">01 / INSTALLATION</span>
    <h2>Install on your Linux distribution</h2>
    <div class="callout">LunaDash is a development preview. Start with a nested session inside the desktop you already use and keep a recovery path.</div>
    <p>The backend needs CMake 3.21+, Ninja, a C11/C++20 compiler, Qt 6.4+, wlroots 0.17–0.20, Wayland development headers and GL/input development libraries. The shell requires Quickshell 0.3 or newer.</p>
    <div class="table-scroll">
      <table class="shortcuts" aria-label="Linux distribution support">
        <thead><tr><th scope="col">Distribution</th><th scope="col">Install path</th></tr></thead>
        <tbody>
          <tr><th scope="row">Arch Linux</th><td>pacman dependencies + makepkg package install</td></tr>
          <tr><th scope="row">Debian / Ubuntu</th><td>apt dependencies + CMake install</td></tr>
          <tr><th scope="row">Fedora</th><td>dnf dependencies + CMake install</td></tr>
          <tr><th scope="row">openSUSE Tumbleweed / Slowroll</th><td>zypper dependencies + CMake install</td></tr>
        </tbody>
      </table>
    </div>
    <p>The dependency helper only uses repositories already enabled on the machine. It never adds a PPA, COPR or OBS repository automatically. If Quickshell is unavailable from the distribution, install it separately from its upstream instructions.</p>
    <CodeBlock label="CHECK / INSTALL DISTRIBUTION DEPENDENCIES" code={dependencies} />
    <CodeBlock label="INSTALL LUNADASH" code={build} />
    <p>Arch keeps the package under pacman ownership. Debian/Ubuntu, Fedora and openSUSE use a standard Release build followed by <code>cmake --install</code> under <code>/usr</code>. Use <code>--skip-deps</code> when your dependencies are already present and <code>--dry-run</code> to inspect commands before changing the system.</p>
    <p>Full login-session preflight checks and recovery steps live in the <a href={`${base}docs/start/`}>run and test guide</a>.</p>
  </section>

  <section id="start" class="wrap section">
    <span class="eyebrow">02 / GETTING STARTED</span>
    <h2>Getting started</h2>
    <p>The first-run guide appears only until it is completed, works offline and saves choices immediately.</p>
    <ol class="steps">
      <li><span>01</span>Choose English, Traditional Chinese, Simplified Chinese or Japanese.</li>
      <li><span>02</span>Review your connection. Existing profiles are reused, and you can continue offline.</li>
      <li><span>03</span>Pick a colour, wallpaper, window gaps and panel behaviour.</li>
      <li><span>04</span>Finish the guide, then open Settings for the complete desktop controls.</li>
    </ol>
    <h3>Keyboard shortcuts</h3>
    <p><strong>Super</strong> is the Meta key. Every binding can be changed or disabled in <strong>Settings → Keyboard shortcuts</strong>.</p>
    <div class="table-scroll">
      <table class="shortcuts" aria-label="Default keyboard shortcuts">
        <thead><tr><th scope="col">Shortcut</th><th scope="col">Action</th></tr></thead>
        <tbody>{shortcuts.map(([keys, action]) => <tr><th scope="row"><code>{keys}</code></th><td>{action}</td></tr>)}</tbody>
      </table>
    </div>
    <p>While you test in a nested session, your host desktop may intercept <code>Super</code> shortcuts first.</p>
  </section>

  <section id="github" class="wrap section">
    <span class="eyebrow">03 / SOURCE</span>
    <h2>Source and releases</h2>
    <p>LunaDash is developed under GPL-3.0-only. Pull requests target the <code>dev</code> branch, and GitHub Release Markdown is automatically rendered into the website release-notes pages.</p>
    <div class="link-list">
      <a href={repository}>Repository <span aria-hidden="true">↗</span></a>
      <a href={`${base}docs/architecture/`}>Source architecture <span aria-hidden="true">↗</span></a>
      <a href={`${repository}/issues`}>Issues <span aria-hidden="true">↗</span></a>
      <a href={`${base}releases/`}>Website release notes <span aria-hidden="true">↗</span></a>
      <a href={`${repository}/releases`}>GitHub Releases <span aria-hidden="true">↗</span></a>
      <a href={`${repository}/blob/main/LICENSE`}>Licence <span aria-hidden="true">↗</span></a>
    </div>
  </section>

  <section id="contributors" class="wrap section">
    <span class="eyebrow">04 / CONTRIBUTORS</span>
    <h2>Contributors</h2>
    <p>LunaDash is maintained by one person, with code, documentation and testing help from the community.</p>
    <ul class="contributors">
      <li><a class="contributor" href={profile}><img src={avatar} alt="LuYishan-4" width="72" height="72" loading="lazy"><span class="contributor-name">LuYishan-4</span><span class="contributor-role">Maintainer</span></a></li>
    </ul>
    <p>Open an issue or a pull request on <a class="inline-link" href={repository}>GitHub</a> to take part.</p>
  </section>
</main>
<footer class="wrap">
  <a class="brand" href="#main"><img src={asset("mark.svg")} alt="" width="25" height="25"> LunaDash</a>
  <span>GPL-3.0-only · Wayland, C++20 and Quickshell.</span>
  <a href={`${base}releases/`}>Release notes <span aria-hidden="true">↗</span></a>
</footer>
</body>
</html>
